# FROZEN SNAPSHOT — pre-v2 (concept-first, budget-allocator) LO pipeline.
Reference / rollback only. NOT wired into the live graph. The live pipeline is the
v2 LO-first flow (m01–m12 nodes). Prompts (prompts/), utils/, config are reused live;
this folder preserves the exact graph/state/artifact/nodes as of the v2 cutover.
