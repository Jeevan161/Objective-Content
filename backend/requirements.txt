# --- Web / API ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- DB / ORM / migrations ---
sqlalchemy>=2.0.30
alembic>=1.13.0
psycopg[binary]>=3.2.0
pgvector>=0.3.0           # SQLAlchemy Vector type + pgvector operators (RAG store)

# --- Settings / env ---
pydantic>=2.0.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0

# --- Numerics (RAG MMR re-rank) ---
numpy>=1.26

# --- Portal scraping (ported verbatim from the Django app) ---
requests>=2.32.0
beautifulsoup4>=4.12.0
soupsieve>=2.5

# --- MCQ generation pipeline (LangGraph) + LangSmith tracing ---
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
anthropic>=0.40.0
langchain-core>=0.3.0
langsmith>=0.1.0

# --- User-configurable LLM providers: encrypt stored API keys at rest (Fernet) ---
cryptography>=42.0.0
